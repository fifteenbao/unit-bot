
 **PLANS 价值设计流程**——一条从现状研究到体系建设的极致降本路径。

| 阶段    | 全称   | 核心命题                            | 输入              | 输出             |
| ----- | ---- | ------------------------------- | --------------- | -------------- |
| **P** | 现状研究 | What is?（现在什么样？）                | 产品实物 / 规格书 / 竞品 | 拆解报告、问题清单、对标基线 |
| **L** | 精益设计 | How to make it lean?（怎么做更精益？）   | P 阶段的拆解数据       | DFA/DFM 优化方案   |
| **A** | 先进裁剪 | What can we cut?（还能裁掉什么？）       | 功能模型 + 成本数据     | 裁剪方案、矛盾解决方案    |
| **N** | 价值创新 | What's the next leap?（下一次跃迁在哪？） | 技术趋势 + 功能缺口     | 新材料/新工艺/新方案    |
| **S** | 体系建设 | How to lock in gains?（怎么固化收益？）  | 前四阶段所有产出        | 平台化方案、成本管理流程   |

### P — 现状研究（Present State Research）

> 目标：建立产品的完整事实基线，不猜测、不假设。

#### **产品定位研究**
- 客户需求分析（MVP 分析）
- 关键指标分析
- 对标分析（Benchmarking）
- 功能分析（正向）
#### **改善机会分析**
- 质量问题清单
- 维修维护问题清单

- 产品拆解及装配流程分析
- 最少件清单
- 装配问题清单

---


### L — 精益设计（Lean Design）

> 目标：通过 DFA + DFM 双轮驱动，消除一切不必要的复杂度和成本。这是 DFMA 方法论的核心着陆点。

#### **DFA 优化（面向装配的设计）**

| #   | 优化方向          |
| --- | ------------- |
| 1   | 最小件为核心的合并     |
| 2   | 紧固件的减少        |
| 3   | 自定位及防呆设计      |
| 4   | 自固定设计         |
| 5   | 防止欠约束与过约束     |
| 5   | 单独装配动作的消除     |
| 6   | 焊接/粘接/螺纹紧固的消除 |
| 7   | 解决人体工学问题      |
| 8   | 标准化设计         |

#### **DFM 优化（面向制造的设计）**

| #   | 优化方向   | 说明            |
| --- | ------ | ------------- |
| 1   | 材料替代   | 更便宜、更易加工的材料   |
| 2   | 工艺改变   | 从高成本工艺转向低成本工艺 |
| 3   | 加工精度放宽 | 只保留功能必要的公差    |
| 4   | 表面处理简化 | 减少不必要的涂装/电镀   |
| 5   | 结构简化   | 减少加工步骤和装夹次数   |
#### **采购降本**

---

### A — 先进裁剪（Advanced Trimming）

> 目标：跳出 DFMA 的渐进式优化，用 TRIZ 工具做激进的结构性裁剪。

#### **功能分析（逆向）**
- 功能建模
- 功能价值分析
- 功能缺陷识别

#### **裁剪策略**

| 等级  | 策略           | 说明           |
| --- | ------------ | ------------ |
| 一级  | 裁剪功能缺陷       | 去掉本就不该存在的功能  |
| 二级  | 裁剪价值 < 1 的组件 | 成本 > 功能价值的部分 |
| 三级  | 激进裁剪         | 把实现功能的载体彻底替换 |

**矛盾解决（TRIZ）**
- 分析解决技术矛盾（一个参数提升导致另一个变差）
- 分析解决物理矛盾（同一参数需要同时满足对立要求）
- 分析解决关键功能缺陷

---

### N — 价值创新（Novel Value Innovation）

> 目标：不只是在现有方案上降本，而是找到全新的价值实现路径。

#### **功能创新**
- 跨领域搜索和筛选功能替代方案（FOS — Function-Oriented Search）
- 做必要的专利规避
- 落实功能实现新方案

#### **四新设计**

| 方向       | 说明             |
| -------- | -------------- |
| 新材料      | 更高性能、更低成本的替代材料 |
| 新工艺      | 更少步骤、更低能耗的制造方式 |
| 新造型      | 更简洁的结构和外观方案    |
| 新控制（智能化） | 用软件/传感器替代机械结构  |

#### **把握趋势**
- S 曲线分析
- 系统进化趋势分析
- 创新方向规划

---

### S — 体系建设（System Building）

> 目标：将前四阶段的降本能力固化到组织能力中，让降本从「一次性项目」变成「持续性能力」。

#### **复杂性管理**
- 产品复杂性分析
- 平台化、模块化设计
- 复杂性管理流程

#### **成本体系建设**

| 维度 | 内容 |
|------|------|
| 组织建设 | 建立 DFMA / 成本工程团队和岗位 |
| 设施建设 | 拆解实验室、成本数据库、工具链 |
| 能力建设 | 培训、认证、方法论内化 |
| 数据建设 | 应该成本模型、工艺成本基准库 |
| 流程建设 | 将 PLANS 嵌入 NPI 流程的关键节点 |

---

## PLANS 多 Agent 执行方案

> 参照 [unit-bot](https://github.com/fifteenbao/unit-bot) 的命令驱动 Agent 模式设计——每个 Agent 边界清晰（做什么 / 不做什么），下游依赖上游产出，形成一条可编排的降本分析流水线。

### 架构总览

```
                    ┌──────────────────────┐
                    │   /plans <product>    │  ← 编排器
                    │   全流程串联执行        │
                    └──────────┬───────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        ▼                      ▼                      ▼
   ┌─────────┐           ┌─────────┐           ┌─────────┐
   │ P 阶段   │           │ L 阶段   │           │ A 阶段   │
   │ 3 Agent │ ────────▶ │ 2 Agent │ ────────▶ │ 2 Agent │
   └─────────┘           └─────────┘           └─────────┘
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                    ┌──────────┴───────────┐
                    ▼                      ▼
               ┌─────────┐           ┌─────────┐
               │ N 阶段   │           │ S 阶段   │
               │ 3 Agent │ ────────▶ │ 2 Agent │
               └─────────┘           └─────────┘
```

### Agent 清单（12 个专职 Agent）

#### P 阶段（现状研究）— 3 Agent

<table>
<tr><th>命令</th><th>Agent</th><th>做什么</th><th>不做什么</th></tr>
<tr>
<td><code>/research &lt;product&gt;</code></td>
<td>产品研究员</td>
<td>多源采集产品定位、客户需求（MVP 分析）、对标竞品、关键指标</td>
<td>不做成本分析、不做拆解</td>
</tr>
<tr>
<td><code>/teardown &lt;product&gt;</code></td>
<td>拆解分析师</td>
<td>产品拆解流程记录、装配流程逆向分析、最少件清单、装配问题清单</td>
<td>不计算成本、不给优化建议</td>
</tr>
<tr>
<td><code>/issues &lt;product&gt;</code></td>
<td>问题诊断师</td>
<td>质量问题清单、维修维护问题清单、改善机会识别</td>
<td>不给解决方案</td>
</tr>
</table>

#### L 阶段（精益设计）— 2 Agent

> ⚠️ 上游依赖：需先跑过 P 阶段的 `/teardown` 和 `/issues`

<table>
<tr><th>命令</th><th>Agent</th><th>做什么</th><th>不做什么</th></tr>
<tr>
<td><code>/dfa &lt;product&gt;</code></td>
<td>DFA 优化师</td>
<td>最小件合并、紧固件减少、自定位防呆、自固定设计、过约束/欠约束检查、装配动作消除、焊接粘接螺纹紧固替代、人体工学评估、标准化建议。<b>装配工时强制查 <code>query_processes</code></b>（P_FAST_SCREW=7s / P_FAST_SNAP=3s / P_BOND=30s / P_WELD_SOLDER=45s），每条节省必带 <code>process_id_ref</code> + <code>saved_formula</code> 公式</td>
<td>不做材料/工艺分析（那是 DFM 的活）</td>
</tr>
<tr>
<td><code>/dfm &lt;product&gt;</code></td>
<td>DFM 优化师 (Should Cost)</td>
<td>材料替代方案、工艺改变评估、加工精度放宽建议、表面处理简化、结构简化。<b>Should Cost 五要素全部查库</b>：① 材料 (query_materials) + ② 加工 (query_processes) + ③ 模具摊销 (query_molds) + ④ 工具折旧 (query_tooling) + ⑤ 8~15% 合理利润。每件 should_cost_analysis 必填 4 个 <code>*_ref</code> 字段追溯查库依据</td>
<td>不做装配分析（那是 DFA 的活）</td>
</tr>
</table>

#### A 阶段（先进裁剪）— 2 Agent

> ⚠️ 上游依赖：需先跑过 L 阶段的 `/dfa` 和 `/dfm`

<table>
<tr><th>命令</th><th>Agent</th><th>做什么</th><th>不做什么</th></tr>
<tr>
<td><code>/function &lt;product&gt;</code></td>
<td>功能建模师</td>
<td>功能建模（逆向）、功能价值分析（价值/成本比）、功能缺陷识别</td>
<td>不做具体裁剪决策</td>
</tr>
<tr>
<td><code>/trim &lt;product&gt;</code></td>
<td>裁剪策略师</td>
<td>裁剪功能缺陷、裁剪价值&lt;1 的组件、激进裁剪路线规划、技术矛盾分析（TRIZ）、物理矛盾分析、关键功能缺陷补救方案</td>
<td>不做新材料/新工艺推荐（那是 N 阶段的活）</td>
</tr>
</table>

#### N 阶段（价值创新）— 3 Agent

> ⚠️ 上游依赖：需先跑过 A 阶段的 `/function` 和 `/trim`

<table>
<tr><th>命令</th><th>Agent</th><th>做什么</th><th>不做什么</th></tr>
<tr>
<td><code>/fos &lt;function&gt;</code></td>
<td>功能创新搜索师</td>
<td>跨领域搜索功能替代方案（FOS）、筛选可行方案、落实功能实现新方案</td>
<td>不做专利法律判断</td>
</tr>
<tr>
<td><code>/patent &lt;solution&gt;</code></td>
<td>专利规避师</td>
<td>专利检索、权利要求映射、规避方案设计</td>
<td>不提供法律意见、不判断侵权</td>
</tr>
<tr>
<td><code>/trend &lt;tech&gt;</code></td>
<td>趋势分析师</td>
<td>S 曲线分析（判断技术成熟度）、系统进化趋势分析、创新方向规划、四新设计机会（新材料/新工艺/新造型/新控制）</td>
<td>不评价具体供应商</td>
</tr>
</table>

#### S 阶段（体系建设）— 2 Agent

> ⚠️ 阶段级依赖：需所有前序阶段产出

<table>
<tr><th>命令</th><th>Agent</th><th>做什么</th><th>不做什么</th></tr>
<tr>
<td><code>/platform &lt;product&gt;</code></td>
<td>平台架构师</td>
<td>产品复杂性分析、平台化模块化方案设计、共用模块识别、复杂性管理流程建议</td>
<td>不做具体零件设计</td>
</tr>
<tr>
<td><code>/costsystem &lt;product&gt;</code></td>
<td>成本体系构建师</td>
<td>组织建设方案、设施建设规划（拆解实验室等）、能力建设路线、成本数据库架构、流程嵌入方案（PLANS → NPI）</td>
<td>不做即时成本核算</td>
</tr>
</table>

---

### 数据流与依赖关系

```
P: /research ─┬─▶ /teardown ─┬─▶ L: /dfa ──┬─▶ A: /function ──┬─▶ N: /fos ──┬─▶ S: /platform
              │              │             │                  │             │
              └─▶ /issues ───┘             ├─▶ /dfm ──┼──────┤             ├─▶ /patent ──┤
                                          │          │      │             │             │
                                          │          └─▶ /trim ───┼─────────┤             ├─▶ /costsystem
                                          │                        │         │             │
                                          │                        │         └─▶ /trend ───┘
                                          │                        │
                                          └─ 采购降本（Should Cost 谈判）◀────────┘
```

> **关键约束**：每个命令都是幂等的——可单独重新运行而不影响已产出的下游结果，只要上游输入未变。

---

### 编排器 `/plans` 工作流

```
/plans <product>

  ┌─ Phase P ─────────────────────────────────────────┐
  │  /research <product>  ──→  产品定位 & 对标报告      │
  │  /teardown <product>  ──→  拆解 & 装配流程报告       │
  │  /issues <product>    ──→  问题清单                 │
  │  产出：事实基线文档                                  │
  └────────────────────────────────────────────────────┘
                          │
  ┌─ Phase L ─────────────────────────────────────────┐
  │  /dfa <product>  ──→  DFA 优化方案                 │
  │  /dfm <product>  ──→  DFM 优化方案                 │
  │  产出：精益设计报告 + 降本潜力估算                    │
  └────────────────────────────────────────────────────┘
                          │
  ┌─ Phase A ─────────────────────────────────────────┐
  │  /function <product>  ──→  功能价值矩阵             │
  │  /trim <product>      ──→  裁剪方案 + TRIZ 分析     │
  │  产出：裁剪路径 + 矛盾解决方向                        │
  └────────────────────────────────────────────────────┘
                          │
  ┌─ Phase N ─────────────────────────────────────────┐
  │  /fos <function>  ──→  替代方案池                  │
  │  /patent <solution> ──→  规避方案                   │
  │  /trend <tech>     ──→  技术趋势 + 四新方向          │
  │  产出：创新机会地图                                  │
  └────────────────────────────────────────────────────┘
                          │
  ┌─ Phase S ─────────────────────────────────────────┐
  │  /platform <product>  ──→  平台化方案               │
  │  /costsystem <product> ──→  体系建设方案             │
  │  产出：组织能力固化方案                               │
  └────────────────────────────────────────────────────┘
                          │
                          ▼
              最终产出：PLANS 综合降本报告
```

---

### 与 unit-bot 的架构对应

#### 旧命令 → 12 子 agent 映射（迁移参考）

历史上 unit-bot 用 `/product` `/bom` `/dfma` 等单一命令完成一类任务，现已**全部废除**，其底层能力按职责拆解到 12 个 PLANS 子 agent 的工具白名单里，由 agent 在执行任务时自主调用。用户只需用 PLANS 12 个 `/{p,l,a,n,s}-*` 命令 + `/plans` 编排器。

| 旧用户命令 (已废除) | 新接管者 | 旧命令底层工具的归属 |
|---------------------|---------|---------------------|
| `/product` 采集规格 | P 阶段 `/research` | `crawl_product_specs` / `web_search` |
| `/bom` 生成拆机 CSV + 7 桶成本 | P 阶段 `/teardown` | `generate_teardown_csv` / `generate_bom_estimate` / `get_bom_cost` |
| `/dfma` 功能-成本矩阵 | A 阶段 `/function` (基础矩阵) + N 阶段 `/fos` (创新方向) | `dfma_analysis` |
| `/vs` 横向对标 | P 阶段 `/research` + N 阶段 `/trend` | `vs_compare` / `compare_by_spec` |
| `/find` 跨机型共件 | S 阶段 `/platform` (做共件率) + L 阶段 `/dfa` (做标准化) | `find_parts` |
| `/cut` 列溢价件 | L 阶段 `/dfm` (Should Cost 谈判 / 溢价件清单) | `cut_premium` |
| `/framework` 7 桶基线导出 | 各 agent 内部调用 | `export_framework` / `dfma_analysis` |
| `/fcc ocr` PCB 芯片提取 | **仍由用户手动跑** (`scripts/fetch_fcc.py`)，`/teardown` 读取产物 | 涉及文件下载+OCR，不在 agent 工具集 |

#### 新增 5 个查库工具（Should Cost 五要素接入）

| Tool | 库 (9 库 ⑥⑦⑧) | 用途 |
|------|---------------|------|
| `query_materials` | ④ `materials.csv` | 拿原材料单价区间 → Should Cost 材料 |
| `query_processes` | ⑥ `processes.csv` | 拿工艺基线 cycle_sec × hourly_rate → Should Cost 加工 |
| `query_molds` | ⑦ `molds.csv` | 拿模具单件摊销 → Should Cost 模具 |
| `query_tooling` | ⑧ `tooling.csv` | 拿夹具/刀具折旧 → Should Cost 工具 |
| `query_suppliers` | ⑤ `suppliers.csv` | 拿供应商档次/采购条件 → 谈判输入 |

这 5 个 query 工具被 L 阶段 `/dfm`、N 阶段 `/fos` `/trend`、S 阶段 `/platform` 共用，让"应该成本（Should Cost）= 材料 + 加工 + 模具摊销 + 工具折旧 + 合理利润"五要素可在任何 agent 内闭环查证。

#### 共性设计哲学

- 命令幂等、边界清晰 ↔ 每个 Agent 有 **做什么 / 不做什么** 的硬约束
- 数据 / 推理 / 报告分层 ↔ 主 agent 负责写库（`plans_db.json`），子 agent 只读不写
- 证据可追溯 ↔ 每个输出字段都有 `evidence_from_*` 引用上游

---

## 核心心法

> **DFMA 不是「降级设计」，而是去除不必要的复杂度，用更智慧的方式实现相同的功能和品质。**

> **设计的简洁不等于简陋，制造的精简不等于妥协。**

> **PLANS 不是五个独立工具，而是一条环环相扣的降本逻辑链——跳过 P 直接做 L 是盲人摸象，做完 A 却不做 N 是半途而废，做完 N 却不做 S 是猴子掰玉米。**